# Development environment overrides for VQMS

environment: development

logging:
  level: DEBUG
  handlers:
    console:
      enabled: true
    cloudwatch:
      enabled: false

postgresql:
  host: localhost
  port: 5432
  database: vqms_dev

redis:
  host: localhost
  port: 6379
