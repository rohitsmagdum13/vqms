# Test environment overrides for VQMS

environment: test

logging:
  level: WARNING
  handlers:
    console:
      enabled: true
    cloudwatch:
      enabled: false

postgresql:
  host: localhost
  port: 5432
  database: vqms_test

redis:
  host: localhost
  port: 6379
  db: 1
